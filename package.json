{
  "name": "payment-card-example-react",
  "version": "1.0.0",
  "description": "",
  "main": "srver/index.js",
  "scripts": {
    "start": "cd server && cd build && node index.js",
    "heroku-postbuild": "cd server && npm install --unsafe-perm && npx tsc --build && cd ../client && npm install --unsafe-perm && npm run build"
  },
  "author": "grdvsng@gmail.com",
  "license": "ISC",
  "engines": {
    "node": "15.x",
    "npm": "7.x"
  },
  "dependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13"
  }
}
