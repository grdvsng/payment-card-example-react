{
  "name": "payment-card-example-react",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd server && npm start && cd ..",
    "heroku-postbuild": "npm install && cd server && npm install && npx tsc --build && cd .. && cd client && npm install && npm run build && cd .. "
  },
  "author": "grdvsng@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13"
  },
  "dependencies": {
    "tsc": "^2.0.4"
  }
}
